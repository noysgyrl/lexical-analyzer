#include <iostream>
#include <vector>

std::string replace(std::string input); //separates commas, semicolons, and parentheses
std::vector<std::string> analyze(std::string in); //returns list of lexemes with types
